# harness 仕様↔実装 整合監査 2026-06-28

## スコープ (全体)
- baseline: `HEAD~20`
- canon commit (HEAD): `3b86f1bce2ff2d7e2fd296c2034ab80d897f5e92`
- 変更ファイル数: 72
- shard: condukt, harness-core, invariants
- 各 shard は独立した read-only エージェント (fresh context) で監査し、ここに統合した。

---

## shard: condukt

# harness 仕様↔実装 整合監査 2026-06-28

## スコープ
- baseline: `HEAD~20`
- 監査した領域: condukt (実装変更 1 件: `crates/condukt/skills/condukt/SKILL.md` / canon 変更 1 件: 同ファイル)
- skip した領域: condukt 以外の全領域 (別 shard / fresh context で監査されるため、本 shard では対象外)
- 不変条件: なし (本 shard では不変条件を扱わない — 別 shard で照合)
- 注記: この shard が照合した正典は `crates/condukt/README.md` と `crates/condukt/skills/condukt/SKILL.md` の 2 点のみ。`fugu-router`/`schemaguard`/`curate`/`evalkit` の subcommand 仕様はそれぞれ別領域の正典が決めるため、本 shard の verdict 対象外とした。

## findings
### D1 実装↔正典 drift
| ルール | 参照正典(file:節) | 逐語引用 | 実装箇所(file:line) | verdict | 分類 | needs_user | 推奨対応 |
|---|---|---|---|---|---|---|---|
| condukt バイナリは「1 ジョブ 1 subcommand」で、新規 condukt subcommand 契約はこの diff で追加されていない | `crates/condukt/README.md`:「What the engine does」導入文 | "The binary is a single Rust executable exposing one subcommand per job." | `SKILL.md`:147-153, 196-207, 393, 405-416 (今回の追加は `fugu-router procedures` / `schemaguard` / `--skill-fingerprint` / `curate` の soft-dep フックのみ) | 整合 | — | no | 対応不要。追加分は全て `command -v` ガード付き外部 soft-dep で、condukt 正典 (README の subcommand 契約) を一切変更・違反していない |
| `fugu-router playbook search` → `procedures search` への改名追従 | (condukt 正典に該当条文なし — fugu-router 領域の正典が支配) | (condukt 正典に逐語引用元なし) | `SKILL.md`:150 | 不明 | 判別不能 | no | 本 shard の canon (condukt README/SKILL) では裁定不能。`fugu-router` 領域 shard が `procedures` 改名 (commit 734a142) との整合を判定する。本 shard では needs_user 化しない (照合不能=修正タスクを起こさない方針) |

### D2 仕様品質 (spec-quality)
| 種別 | 該当(file:節, 複数可) | 逐語引用 | needs_user | 推奨対応 |
|---|---|---|---|---|
| 重複 | `crates/condukt/README.md`:「Source of truth: edit the repo, not the cache」 / `crates/condukt/skills/condukt/SKILL.md`:「失敗モード — condukt 自身を改修する場合」(562-568) | README: "Editing that cache copy — easy to do by accident when you use condukt to improve condukt itself — produces edits that live outside git and silently diverge from the repo." / SKILL: "**install キャッシュ (`~/.claude/plugins/cache/.../condukt/...`) を worker に編集させない** — キャッシュ編集は git 外でリポジトリと黙って乖離し、新規 install で消える。" | yes | 同一の真実 (キャッシュ直接編集禁止 + `scripts/sync-plugin-assets.sh` で同期) が 2 正典に実体コピーされている。片方を正典に決め、他方は参照リンクに置換 (1事実1正典化、ドリフトの種を除去) |
| 沈黙 | `crates/condukt/README.md`:「What the engine does」subcommand 表 (vs `SKILL.md` の参照) | README 表は `condukt state` 行を "`condukt state init/set/show/gate/list`" + stats/reconcile/resume-context/test に限定。一方 SKILL は "condukt state conflict-check --file <json.routed>" (239), "condukt state abandon --run $RID --all-stuck" (60), "condukt state list-tasks" (518), "condukt state cancel --run <run_id> --task <task_id>" (544), "condukt state pause --run <conflict_run_id>" (268), "KNOWLEDGE=$(condukt knowledge 2>/dev/null || true)" (142) を使用 | yes | SKILL が依存する `state conflict-check` / `state abandon` / `state list-tasks` / `state cancel` / `state pause` / `knowledge` が README の subcommand 表に未記載 (canon の沈黙)。README 表に追記して「skill が呼ぶ全 subcommand を canon が定義」を満たす |

## 修正候補 (needs_user=yes / 承認待ち)
- [D2] 重複 / 該当: `crates/condukt/README.md`「Source of truth」節 ↔ `crates/condukt/skills/condukt/SKILL.md`「失敗モード — condukt 自身を改修する場合」(562-568) / 推奨対応: キャッシュ直接編集禁止ルールを片方の正典に集約し、他方は参照に置換
- [D2] 沈黙 / 該当: `crates/condukt/README.md`「What the engine does」subcommand 表 / 推奨対応: SKILL が参照する `state conflict-check`・`state abandon`・`state list-tasks`・`state cancel`・`state pause`・`knowledge` を README 表に追記

---

## shard: harness-core

# harness 仕様↔実装 整合監査 2026-06-28

## スコープ
- baseline: `HEAD~20`
- 監査した領域: harness-core（実装変更 4 件: `crates/harness-core/src/gate/run.rs`, `crates/harness-core/src/inject.rs`, `crates/harness-core/src/lib.rs`, `crates/harness-core/src/usage.rs`。正典ポインタ: `docs/OVERVIEW.md`）
- skip した領域: harness-core 以外の全領域（別 shard / fresh context で監査。本 shard の対象外）
- 不変条件: なし（本 shard では不変条件を扱わない旨が shard プロンプトに明記。不変条件は別 shard で照合）

## findings
### D1 実装↔正典 drift
| ルール | 参照正典(file:節) | 逐語引用 | 実装箇所(file:line) | verdict(整合/矛盾/仕様未記載/不明) | 分類(A/B/C/判別不能) | needs_user(yes/no) | 推奨対応 |
|---|---|---|---|---|---|---|---|
| 不変条件「never break a turn」: コアのログ/フック処理はバグでもターンを壊してはならない | docs/OVERVIEW.md:設計原則2（フック不変条件） | 「どのプラグインのバグも Claude Code のターンを壊さない、という不変条件がコアライブラリに焼き込まれている。」 | crates/harness-core/src/gate/run.rs:60-80（`append_jsonl`。doc: "Best-effort — a serialization or IO failure is swallowed, since an observability log must never break the turn it records."） | 整合 | - | no | なし（best-effort で IO/serialize 失敗を握りつぶし、不変条件を維持） |
| トランスクリプトのフル読み込み禁止・常にストリーミング | docs/OVERVIEW.md:設計原則3（Subscription-native） | 「メモリ上限を守るためトランスクリプトのフル読み込みは禁止し、常にストリーミング処理する。」 | crates/harness-core/src/inject.rs:30-49（`load_layered` が `std::fs::read_to_string`） | 整合 | - | no | なし（読むのは config TOML であってトランスクリプトではない。正典の禁止対象に当たらない） |
| playbook / runbook の役割（playbook=関連ノート自動注入、runbook=`!name` マクロ展開） | docs/OVERVIEW.md:コンテキスト管理（playbook 節 / run-book 節） | 「プロジェクト固有の知識（`.playbook/*.md`）を…関連ノートを自動注入する。」「プロンプト中の `!name` を `.runbook/<name>.md` の内容に展開して注入する。」 | crates/harness-core/src/inject.rs:1-13（module doc: "playbook auto-selects relevant notes; runbook expands explicit `!name` macros"） | 整合 | - | no | なし（共有 substrate の役割記述が正典の plugin 記述と一致） |

### D2 仕様品質 (spec-quality)
| 種別(沈黙/矛盾/重複) | 該当(file:節, 複数可) | 逐語引用 | needs_user | 推奨対応 |
|---|---|---|---|---|
| 沈黙 | docs/OVERVIEW.md:「共通基盤 — harness-core」モジュール表（lines 38-46）／ crates/harness-core/src/lib.rs:17（`pub mod inject;`） | 正典表の現行行は「`hook.rs` / `store.rs` / `transcript.rs` / `pricing.rs` / `install.rs` / `interrogate.rs`」のみ。コード側: 「pub mod inject;」（module doc: "Shared substrate for the context-injection plugins (`playbook`, `runbook`)."） | yes | 新規追加された共有 substrate モジュール `inject`（playbook/runbook 共通基盤）が正典のモジュール表に未掲載。表へ 1 行追加を検討。なお同表は既に `gate` / `daily` / `ledger` / `usage` 等も省いた非網羅な curated 表であり、表を網羅管理する方針か curated に留めるかは人間判断（daily は line 200 で別途 `harness-core::daily::DailyGuard` と言及あり、扱いが不統一） |

## 修正候補 (needs_user=yes / 承認待ち)
- [D2] 沈黙 / 該当: docs/OVERVIEW.md:「共通基盤 — harness-core」モジュール表（lines 38-46） / 推奨対応: 新規 `inject` モジュール（playbook/runbook の共有 substrate）を表に追記するか検討。併せて同表が既に `gate`/`daily`/`ledger`/`usage` を欠く非網羅表である点（網羅維持 or curated 方針）を人間が決定。

---

## shard: invariants

# harness 仕様↔実装 整合監査 2026-06-28

## スコープ
- baseline: HEAD~20
- 監査した領域: なし (D1 領域監査は別 shard 担当)
- skip した領域: D1 全領域 — 本 shard はプロンプト指示により不変条件のみに集中 (32-34 行目)
- 不変条件: **hook never panics** (`harness-core::hook::run_hook` がパニックを捕捉して常に exit 0)

## findings
### D1 実装↔正典 drift
| ルール | 参照正典(file:節) | 逐語引用 | 実装箇所(file:line) | verdict(整合/矛盾/仕様未記載/不明) | 分類(A/B/C/判別不能) | needs_user(yes/no) | 推奨対応 |
|---|---|---|---|---|---|---|---|
| hook はパニックを捕捉し常に exit 0 で返す (ターンを壊さない) | docs/OVERVIEW.md:§3つの設計原則-2 フック不変条件 | 「`harness-core::hook::run_hook` がパニックを捕捉して常に exit 0 で返す。どのプラグインのバグも Claude Code のターンを壊さない、という不変条件がコアライブラリに焼き込まれている。」 | crates/harness-core/src/hook.rs:85-88 | 整合 | — | no | なし。`run_hook` は `catch_silent(f)`（内部で `std::panic::catch_unwind`）でパニックを呑み込み、その戻り値を無視して無条件に `std::process::exit(0)` を呼ぶ。正典の「捕捉して常に exit 0」に逐語的に一致。 |

補足 (取り下げ/分類 A 相当、行は出さない): `read_stdin` (71-75) と `HookInput::parse` (51-57) も I/O エラー・空入力・不正 JSON を呑み込んで silent に倒す設計で、不変条件「never break a turn」を補強しており矛盾なし。

### D2 仕様品質 (spec-quality)
| 種別(沈黙/矛盾/重複) | 該当(file:節, 複数可) | 逐語引用 | needs_user | 推奨対応 |
|---|---|---|---|---|
| (該当なし) | — | — | no | — |

注: hook.rs の module doc コメント (4-6 行目「a hook must NEVER break the user's turn. On any error or panic we exit 0 and stay silent. `run_hook` enforces that.」) は OVERVIEW.md §2 の不変条件を英語で言い換えているが、実装直近のコードコメントであり「正典 doc 間の実体コピー」には当たらないため重複 finding には昇格しない (機械判定可能な 1事実2正典コピーではない)。

## 修正候補 (needs_user=yes / 承認待ち)
- なし
