# harness 仕様↔実装 整合監査 2026-06-26

## スコープ (全体)
- baseline: `HEAD~20`
- canon commit (HEAD): `7d732ec1d2b3b86a6831ad44d2463616c5199e3b`
- 変更ファイル数: 21
- shard: condukt, invariants
- 各 shard は独立した read-only エージェント (fresh context) で監査し、ここに統合した。

---

## shard: condukt

# harness 仕様↔実装 整合監査 2026-06-26

## スコープ
- baseline: HEAD~20
- 監査した領域: condukt (`crates/condukt/README.md`, `crates/condukt/skills/condukt/SKILL.md`)
- skip した領域: なし (この shard は condukt のみ担当)
- 不変条件: この shard では扱わない (別 shard で照合)

## findings
### D1 実装↔正典 drift
| ルール | 参照正典(file:節) | 逐語引用 | 実装箇所(file:line) | verdict(整合/矛盾/仕様未記載/不明) | 分類(A/B/C/判別不能) | needs_user(yes/no) | 推奨対応 |
|---|---|---|---|---|---|---|---|
| interpreter が出力する Decomposition JSON のトップレベルに `linked_hypotheses` フィールドを含める | `crates/condukt/skills/condukt/SKILL.md` / Phase 1 | `{ "goal": "...", "linked_hypotheses": ["hid1", "hid2"], "tasks": [` | `crates/condukt/agents/condukt-interpreter.md` L24-41 (出力スキーマ定義) | 矛盾 | B/C 判別不能 | yes | interpreter agent の出力スキーマに `linked_hypotheses` を追加するか、SKILL.md の schema 表示を「soft optional — interpreter 非対応時は省略」と明記して整合性を取るか、人間が判断する |
| Phase 0-next で `OPEN_HYPOS` を取得し仮説コンテキストを活用する | `crates/condukt/skills/condukt/SKILL.md` / Phase 0-next | `OPEN_HYPOS=$(hypothesis list --status open 2>/dev/null | head -10 || true)` および `取得した $OPEN_HYPOS（open 仮説一覧）も文脈として活用する。` | `crates/condukt/skills/condukt/SKILL.md` 自身 (skill は LLM が実行する手順書) | 整合 | — | no | SKILL.md 自体が実装 (skill) であり、変更はこのファイル内で完結している |
| Phase 8 の仮説自動クローズ・spec-drift チェックは gate PASS 後にのみ実行する | `crates/condukt/skills/condukt/SKILL.md` / Phase 8 | `**仮説の自動クローズ (soft 依存)**: gate PASS 後、Phase 1 で interpreter が記録した linked_hypotheses を自動 validate する` および `**spec-drift findings は condukt 完了を阻害しない**` | `crates/condukt/skills/condukt/SKILL.md` 自身 | 整合 | — | no | — |

### D2 仕様品質 (spec-quality)
| 種別(沈黙/矛盾/重複) | 該当(file:節, 複数可) | 逐語引用 | needs_user | 推奨対応 |
|---|---|---|---|---|
| 矛盾 | `crates/condukt/skills/condukt/SKILL.md` / Phase 1 と `crates/condukt/agents/condukt-interpreter.md` / 返す形 | SKILL.md: `{ "goal": "...", "linked_hypotheses": ["hid1", "hid2"], "tasks": [` / interpreter.md: `"goal": "一文の到達目標", "tasks": [` (linked_hypotheses なし) / SKILL.md 同箇所: `スキーマは agents/condukt-interpreter.md 準拠` | yes | SKILL.md が「スキーマは `agents/condukt-interpreter.md` 準拠」と明記しているにもかかわらず両者のスキーマが乖離している。`condukt-interpreter.md` に `linked_hypotheses` (省略可) を追加して一致させること |
| 矛盾 | `crates/condukt/README.md` / decomposition schema と `crates/condukt/skills/condukt/SKILL.md` / Phase 1 | README.md: `{ "goal": "...", "tasks": [` (linked_hypotheses なし) / SKILL.md Phase 1: `{ "goal": "...", "linked_hypotheses": ["hid1", "hid2"], "tasks": [` | yes | README.md のスキーマ例に `linked_hypotheses` フィールドを追加する (または「`linked_hypotheses` は hypothesis プラグイン使用時のみ付与される省略可能フィールド」と注記する) |
| 重複 | `crates/condukt/README.md` / decomposition schema と `crates/condukt/skills/condukt/SKILL.md` / Phase 1 と `crates/condukt/agents/condukt-interpreter.md` / 返す形 | 三箇所に Decomposition JSON スキーマの実体コピーが存在し、それぞれが微妙に異なる内容を持つ (1事実1正典違反) | yes | スキーマの正典を1箇所 (例: `condukt-interpreter.md`) に集約し、他箇所は参照のみとするか、少なくとも更新手順をドキュメント化する |

## 修正候補 (needs_user=yes / 承認待ち)
- [D1] `linked_hypotheses` フィールドが SKILL.md の Decomposition JSON スキーマに追加されたが interpreter agent の出力スキーマに反映されていない / 実装箇所: `crates/condukt/agents/condukt-interpreter.md` L24-41 / 正典: `crates/condukt/skills/condukt/SKILL.md` / Phase 1 / 分類: B/C 判別不能 / 推奨対応: interpreter agent のスキーマに `linked_hypotheses` (省略可、hypothesis プラグイン非存在時) を追加するか、SKILL.md に「interpreter が出力しない場合は主体 (main skill) が付与する」と明記して役割を明確化する
- [D2] 矛盾 / 該当: `crates/condukt/skills/condukt/SKILL.md` / Phase 1 vs `crates/condukt/agents/condukt-interpreter.md` / 返す形 / 推奨対応: `condukt-interpreter.md` の出力スキーマに `"linked_hypotheses": ["関連仮説のid (省略可)"]` を追加し、SKILL.md との整合を取る
- [D2] 矛盾 / 該当: `crates/condukt/README.md` / decomposition schema vs `crates/condukt/skills/condukt/SKILL.md` / Phase 1 / 推奨対応: README.md のスキーマ例に `linked_hypotheses` フィールドを追加する (または「`linked_hypotheses` は hypothesis プラグイン使用時のみ付与される省略可能フィールド」と注記する)
- [D2] 重複 / 該当: `crates/condukt/README.md`, `crates/condukt/skills/condukt/SKILL.md`, `crates/condukt/agents/condukt-interpreter.md` / 推奨対応: Decomposition JSON スキーマの正典を1ファイルに集約し (最有力候補: `condukt-interpreter.md`)、他箇所は参照のみとするリファクタリングを検討する

---

## shard: invariants

# harness 仕様↔実装 整合監査 2026-06-26

## スコープ
- baseline: HEAD~20
- 監査した領域: なし (D1 領域監査は別 shard)
- skip した領域: D1 領域監査全件 — 別 shard で実施
- 不変条件: hook never panics (`harness-core::hook::run_hook` がパニックを捕捉して常に exit 0 で返す)

## findings
### D1 実装↔正典 drift
| ルール | 参照正典(file:節) | 逐語引用 | 実装箇所(file:line) | verdict | 分類 | needs_user | 推奨対応 |
|---|---|---|---|---|---|---|---|
| hook never panics — run_hook はパニックを捕捉して常に exit 0 で返す | `docs/OVERVIEW.md` §2 フック不変条件, `crates/harness-core/src/hook.rs` モジュール doc | OVERVIEW.md: 「`harness-core::hook::run_hook` がパニックを捕捉して常に exit 0 で返す」/ hook.rs L84-88: `pub fn run_hook<F: FnOnce() + UnwindSafe>(f: F) -> ! { let _ = catch_silent(f); std::process::exit(0); }` / hook.rs L79-81: `pub fn catch_silent<F: FnOnce() + UnwindSafe>(f: F) -> bool { std::panic::catch_unwind(f).is_ok() }` | `crates/harness-core/src/hook.rs:85-88` | 整合 | — | no | 対応不要 |

### D2 仕様品質 (spec-quality)
| 種別 | 該当(file:節, 複数可) | 逐語引用 | needs_user | 推奨対応 |
|---|---|---|---|---|
| (なし) | — | — | — | — |

## 修正候補 (needs_user=yes / 承認待ち)
なし
