{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash|Edit|MultiEdit|Write|Read|Grep|Glob",
        "hooks": [
          { "type": "command", "command": "${CLAUDE_PLUGIN_ROOT}/bin/stuckguard watch", "timeout": 10 }
        ]
      }
    ]
  }
}
