//! `integration-tests` — a content-free lib crate whose only purpose is to host
//! the cross-crate E2E integration tests under `tests/`. Those tests spawn the
//! REAL built workspace binaries to pin the flow-pipeline "integration contract".
//! There is intentionally no library code here.
