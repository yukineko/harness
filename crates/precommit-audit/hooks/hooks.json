{
  "hooks": {
    "SessionEnd": [
      {
        "hooks": [
          { "type": "command", "command": "${CLAUDE_PLUGIN_ROOT}/bin/precommit-audit --mode stop", "timeout": 30 }
        ]
      }
    ]
  }
}
