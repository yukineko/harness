{
  "name": "reviewgate",
  "version": "0.1.2",
  "description": "Code-review reporter for Claude Code: on SessionEnd, review the diff and surface findings — in inject mode it emits a review rubric, in subprocess mode it runs an independent reviewer — as an advisory at session end. Subscription-native, bundled Rust binary.",
  "author": { "name": "yukineko" },
  "keywords": ["hooks", "code-review", "quality", "gate", "rust"]
}
