/target
**/*.rs.bk
# Cargo.lock IS committed: specguard ships a binary, so a pinned lockfile gives
# reproducible builds.
# specguard's own outputs when dogfooding
/reports
/.specguard-pending
# /specguard:run の一時 ingest ファイル
/.specguard-ingest.json
